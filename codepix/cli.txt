docker-compose up -d = roda auto todos containers

docker-compose ps = listar container

docker exec -it codepix_app_1 bash = entrar no container via bash

bash> ls ou dir = ver os arvivos do container

bash> go mod init github.com/HRsniper/imersao-fullstack-fullcycle = init e vai baixar auto tods pacotes que for add

docker exec -it codepix_app_1 bash = entrar no container via bash

bash> evans -r repl = client grpc
    bash> call RegisterPixKey

bash> cobra init --pkg-name codepix
bash> cobra add grpc

bash> go run main.go grpc

bash> cobra add kafka

docker exec -it codepix_kafka_1 bash

navegador> localhost:9021 = control center para ver/criar os topicos do kafka sem o  cli

bash> cobra add all

bash> cobra add fixtures
